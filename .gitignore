node_modules
.pnpm-store
packages/presentation-author/.sandbox-runtime
.next/
out/
.turbo/
dist
coverage
cdk.out
playwright-report/
test-results/
*.tsbuildinfo
asset-local-*.png
.DS_Store
.tmp
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.worktree
.envrc.local
.artifacts/
.playwright-mcp

# Generated by synthesizeComponents() in component-catalog.ts (not used by current runtime)
templates/components/
.worktrees/
everything-claude-code/
.devbox/

# Stage-scoped CDK outputs and generated env files (never commit — contain
# account IDs, ARNs, and table names). Both subdirectories are gitignored:
#   .agentra/outputs/<stage>.json  written by `just cdk-deploy-with-outputs`
#   .agentra/env/<stage>/<target>.env  written by `just outputs-env`
# See docs/development/cdk-verify.md and docs/development/dev-modes.md.
.agentra/
